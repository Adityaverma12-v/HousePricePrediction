# Database Configuration
# Update these credentials based on your MySQL setup

db.url=jdbc:mysql://localhost:3306/houseprice_db
db.user=root
db.password=password
db.driver=com.mysql.cj.jdbc.Driver

# Connection Pool Settings
db.pool.initial=5
db.pool.max=20
db.pool.min=2

# Application Settings
app.name=House Price Prediction System
app.version=1.0.0

# Thread Pool Configuration
thread.pool.size=4
thread.timeout=10

# Cache Settings
cache.enabled=true
cache.size=100
